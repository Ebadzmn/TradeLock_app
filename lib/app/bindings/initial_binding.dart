import 'package:get/get.dart';

class InitialBinding extends Bindings {
  @override
  void dependencies() {
    // Inject global dependencies here (e.g., ApiClient, SharedPreferences)
    // Get.put(ApiClient(), permanent: true);
  }
}
